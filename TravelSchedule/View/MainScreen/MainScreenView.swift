import SwiftUI

struct MainScreenView: View {
    var body: some View {
        VStack {
            Color.redUniversal
                .ignoresSafeArea(edges: .top)
        }
    }
}
 
#Preview {
    MainScreenView()
}
